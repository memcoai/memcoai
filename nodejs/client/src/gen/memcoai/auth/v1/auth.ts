// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v3.21.12
// source: memcoai/auth/v1/auth.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "memcoai.auth.v1";

/**
 * IssueTokenRequest carries the credentials and, optionally, a narrower scope and
 * a lifetime.
 */
export interface IssueTokenRequest {
  /** grant_type must be "client_credentials"; no other grant is implemented. */
  grantType: string;
  /** client_id identifies the API client, as issued when it was created. */
  clientId: string;
  /**
   * client_secret is the secret issued with it, which is never a bearer
   * credential itself.
   */
  clientSecret: string;
  /**
   * scope narrows the token to a subset of what the credential holds. Empty
   * takes all of them; anything wider is refused.
   */
  scope: string[];
  /**
   * ttl_seconds is the lifetime asked for. Zero takes the default of 3600; more
   * than 86400 is refused.
   */
  ttlSeconds: number;
}

/** IssueTokenResponse is the minted token, in the terms RFC 6749 §5.1 states. */
export interface IssueTokenResponse {
  /** access_token is sent as "Authorization: Bearer <access_token>". */
  accessToken: string;
  /** token_type is always "Bearer". */
  tokenType: string;
  /** expires_in is the token's lifetime in seconds. */
  expiresIn: number;
  /** scope is what the token actually carries. */
  scope: string[];
}

function createBaseIssueTokenRequest(): IssueTokenRequest {
  return { grantType: "", clientId: "", clientSecret: "", scope: [], ttlSeconds: 0 };
}

export const IssueTokenRequest: MessageFns<IssueTokenRequest> = {
  encode(message: IssueTokenRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.grantType !== "") {
      writer.uint32(10).string(message.grantType);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.clientSecret !== "") {
      writer.uint32(26).string(message.clientSecret);
    }
    for (const v of message.scope) {
      writer.uint32(34).string(v!);
    }
    if (message.ttlSeconds !== 0) {
      writer.uint32(40).int32(message.ttlSeconds);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssueTokenRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssueTokenRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.grantType = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.clientId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.clientSecret = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scope.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.ttlSeconds = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssueTokenRequest {
    return {
      grantType: isSet(object.grantType) ? globalThis.String(object.grantType) : "",
      clientId: isSet(object.clientId) ? globalThis.String(object.clientId) : "",
      clientSecret: isSet(object.clientSecret) ? globalThis.String(object.clientSecret) : "",
      scope: globalThis.Array.isArray(object?.scope) ? object.scope.map((e: any) => globalThis.String(e)) : [],
      ttlSeconds: isSet(object.ttlSeconds) ? globalThis.Number(object.ttlSeconds) : 0,
    };
  },

  toJSON(message: IssueTokenRequest): unknown {
    const obj: any = {};
    if (message.grantType !== "") {
      obj.grantType = message.grantType;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.clientSecret !== "") {
      obj.clientSecret = message.clientSecret;
    }
    if (message.scope?.length) {
      obj.scope = message.scope;
    }
    if (message.ttlSeconds !== 0) {
      obj.ttlSeconds = Math.round(message.ttlSeconds);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IssueTokenRequest>, I>>(base?: I): IssueTokenRequest {
    return IssueTokenRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IssueTokenRequest>, I>>(object: I): IssueTokenRequest {
    const message = createBaseIssueTokenRequest();
    message.grantType = object.grantType ?? "";
    message.clientId = object.clientId ?? "";
    message.clientSecret = object.clientSecret ?? "";
    message.scope = object.scope?.map((e) => e) || [];
    message.ttlSeconds = object.ttlSeconds ?? 0;
    return message;
  },
};

function createBaseIssueTokenResponse(): IssueTokenResponse {
  return { accessToken: "", tokenType: "", expiresIn: 0, scope: [] };
}

export const IssueTokenResponse: MessageFns<IssueTokenResponse> = {
  encode(message: IssueTokenResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (message.tokenType !== "") {
      writer.uint32(18).string(message.tokenType);
    }
    if (message.expiresIn !== 0) {
      writer.uint32(24).int32(message.expiresIn);
    }
    for (const v of message.scope) {
      writer.uint32(34).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssueTokenResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssueTokenResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tokenType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.expiresIn = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.scope.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssueTokenResponse {
    return {
      accessToken: isSet(object.accessToken) ? globalThis.String(object.accessToken) : "",
      tokenType: isSet(object.tokenType) ? globalThis.String(object.tokenType) : "",
      expiresIn: isSet(object.expiresIn) ? globalThis.Number(object.expiresIn) : 0,
      scope: globalThis.Array.isArray(object?.scope) ? object.scope.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: IssueTokenResponse): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.tokenType !== "") {
      obj.tokenType = message.tokenType;
    }
    if (message.expiresIn !== 0) {
      obj.expiresIn = Math.round(message.expiresIn);
    }
    if (message.scope?.length) {
      obj.scope = message.scope;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IssueTokenResponse>, I>>(base?: I): IssueTokenResponse {
    return IssueTokenResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IssueTokenResponse>, I>>(object: I): IssueTokenResponse {
    const message = createBaseIssueTokenResponse();
    message.accessToken = object.accessToken ?? "";
    message.tokenType = object.tokenType ?? "";
    message.expiresIn = object.expiresIn ?? 0;
    message.scope = object.scope?.map((e) => e) || [];
    return message;
  },
};

/**
 * TokenService turns an API client's credentials into a bearer token.
 *
 * It is the one service called without a token of its own — the credentials in
 * the request are what authenticate it — so it lives apart from the services that
 * require one, and a client can bootstrap over gRPC with no HTTP client at all.
 * The same exchange is served over REST at /api/external/v1/oauth/token.
 */
export type TokenServiceService = typeof TokenServiceService;
export const TokenServiceService = {
  /**
   * IssueToken performs the OAuth 2.0 client-credentials grant (RFC 6749 §4.4).
   *
   * The token is a bearer credential for the operations the client was created
   * for, and expires on its own. Every credential failure — an unknown client, a
   * wrong secret, a retired one — is the same UNAUTHENTICATED answer, so nothing
   * about which part was wrong is disclosed.
   */
  issueToken: {
    path: "/memcoai.auth.v1.TokenService/IssueToken",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: IssueTokenRequest) => Buffer.from(IssueTokenRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => IssueTokenRequest.decode(value),
    responseSerialize: (value: IssueTokenResponse) => Buffer.from(IssueTokenResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => IssueTokenResponse.decode(value),
  },
} as const;

export interface TokenServiceServer extends UntypedServiceImplementation {
  /**
   * IssueToken performs the OAuth 2.0 client-credentials grant (RFC 6749 §4.4).
   *
   * The token is a bearer credential for the operations the client was created
   * for, and expires on its own. Every credential failure — an unknown client, a
   * wrong secret, a retired one — is the same UNAUTHENTICATED answer, so nothing
   * about which part was wrong is disclosed.
   */
  issueToken: handleUnaryCall<IssueTokenRequest, IssueTokenResponse>;
}

export interface TokenServiceClient extends Client {
  /**
   * IssueToken performs the OAuth 2.0 client-credentials grant (RFC 6749 §4.4).
   *
   * The token is a bearer credential for the operations the client was created
   * for, and expires on its own. Every credential failure — an unknown client, a
   * wrong secret, a retired one — is the same UNAUTHENTICATED answer, so nothing
   * about which part was wrong is disclosed.
   */
  issueToken(
    request: IssueTokenRequest,
    callback: (error: ServiceError | null, response: IssueTokenResponse) => void,
  ): ClientUnaryCall;
  issueToken(
    request: IssueTokenRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: IssueTokenResponse) => void,
  ): ClientUnaryCall;
  issueToken(
    request: IssueTokenRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: IssueTokenResponse) => void,
  ): ClientUnaryCall;
}

export const TokenServiceClient = makeGenericClientConstructor(
  TokenServiceService,
  "memcoai.auth.v1.TokenService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): TokenServiceClient;
  service: typeof TokenServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
