// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               v3.21.12
// source: memcoai/memory/v1/memory.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "memcoai.memory.v1";

/**
 * DataSource names who produced the content. The operator source is written
 * only by the memory operators and is deliberately absent: it is not a value a
 * caller may claim.
 */
export enum DataSource {
  /** DATA_SOURCE_UNSPECIFIED - DATA_SOURCE_UNSPECIFIED is read as DATA_SOURCE_AGENT. */
  DATA_SOURCE_UNSPECIFIED = 0,
  DATA_SOURCE_USER = 1,
  DATA_SOURCE_AGENT = 2,
  UNRECOGNIZED = -1,
}

export function dataSourceFromJSON(object: any): DataSource {
  switch (object) {
    case 0:
    case "DATA_SOURCE_UNSPECIFIED":
      return DataSource.DATA_SOURCE_UNSPECIFIED;
    case 1:
    case "DATA_SOURCE_USER":
      return DataSource.DATA_SOURCE_USER;
    case 2:
    case "DATA_SOURCE_AGENT":
      return DataSource.DATA_SOURCE_AGENT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DataSource.UNRECOGNIZED;
  }
}

export function dataSourceToJSON(object: DataSource): string {
  switch (object) {
    case DataSource.DATA_SOURCE_UNSPECIFIED:
      return "DATA_SOURCE_UNSPECIFIED";
    case DataSource.DATA_SOURCE_USER:
      return "DATA_SOURCE_USER";
    case DataSource.DATA_SOURCE_AGENT:
      return "DATA_SOURCE_AGENT";
    case DataSource.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * RevertOutcome reports what a revert actually removed, so the caller is told
 * what happened rather than what it had asked for. It names the effect, not the
 * originating operation.
 */
export enum RevertOutcome {
  REVERT_OUTCOME_UNSPECIFIED = 0,
  /**
   * REVERT_OUTCOME_MEMORY_REMOVED - The reverted insight was its memory's last, so the memory and its orphaned
   * intents went with it.
   */
  REVERT_OUTCOME_MEMORY_REMOVED = 1,
  /** REVERT_OUTCOME_ADDITION_REMOVED - Only the caller's insight was removed — its memory holds others and stays. */
  REVERT_OUTCOME_ADDITION_REMOVED = 2,
  /**
   * REVERT_OUTCOME_ENTRY_REMOVED - The caller's insight was removed and no memory was involved either way — a
   * write the validator rejected never reaches one.
   */
  REVERT_OUTCOME_ENTRY_REMOVED = 3,
  /**
   * REVERT_OUTCOME_MERGED - The operation was folded into an existing insight as an endorsement rather
   * than added as new. The caller's duplicate was removed; the endorsed insight
   * is untouched.
   */
  REVERT_OUTCOME_MERGED = 4,
  /**
   * REVERT_OUTCOME_NOT_FOUND - No write by this caller carries the operation id — either it never existed
   * or its ingestion has not completed yet.
   */
  REVERT_OUTCOME_NOT_FOUND = 5,
  /** REVERT_OUTCOME_EXPIRED - The operation is older than the revert window. */
  REVERT_OUTCOME_EXPIRED = 6,
  /** REVERT_OUTCOME_REFUSED - The content is under moderation and only a moderator may remove it. */
  REVERT_OUTCOME_REFUSED = 7,
  UNRECOGNIZED = -1,
}

export function revertOutcomeFromJSON(object: any): RevertOutcome {
  switch (object) {
    case 0:
    case "REVERT_OUTCOME_UNSPECIFIED":
      return RevertOutcome.REVERT_OUTCOME_UNSPECIFIED;
    case 1:
    case "REVERT_OUTCOME_MEMORY_REMOVED":
      return RevertOutcome.REVERT_OUTCOME_MEMORY_REMOVED;
    case 2:
    case "REVERT_OUTCOME_ADDITION_REMOVED":
      return RevertOutcome.REVERT_OUTCOME_ADDITION_REMOVED;
    case 3:
    case "REVERT_OUTCOME_ENTRY_REMOVED":
      return RevertOutcome.REVERT_OUTCOME_ENTRY_REMOVED;
    case 4:
    case "REVERT_OUTCOME_MERGED":
      return RevertOutcome.REVERT_OUTCOME_MERGED;
    case 5:
    case "REVERT_OUTCOME_NOT_FOUND":
      return RevertOutcome.REVERT_OUTCOME_NOT_FOUND;
    case 6:
    case "REVERT_OUTCOME_EXPIRED":
      return RevertOutcome.REVERT_OUTCOME_EXPIRED;
    case 7:
    case "REVERT_OUTCOME_REFUSED":
      return RevertOutcome.REVERT_OUTCOME_REFUSED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RevertOutcome.UNRECOGNIZED;
  }
}

export function revertOutcomeToJSON(object: RevertOutcome): string {
  switch (object) {
    case RevertOutcome.REVERT_OUTCOME_UNSPECIFIED:
      return "REVERT_OUTCOME_UNSPECIFIED";
    case RevertOutcome.REVERT_OUTCOME_MEMORY_REMOVED:
      return "REVERT_OUTCOME_MEMORY_REMOVED";
    case RevertOutcome.REVERT_OUTCOME_ADDITION_REMOVED:
      return "REVERT_OUTCOME_ADDITION_REMOVED";
    case RevertOutcome.REVERT_OUTCOME_ENTRY_REMOVED:
      return "REVERT_OUTCOME_ENTRY_REMOVED";
    case RevertOutcome.REVERT_OUTCOME_MERGED:
      return "REVERT_OUTCOME_MERGED";
    case RevertOutcome.REVERT_OUTCOME_NOT_FOUND:
      return "REVERT_OUTCOME_NOT_FOUND";
    case RevertOutcome.REVERT_OUTCOME_EXPIRED:
      return "REVERT_OUTCOME_EXPIRED";
    case RevertOutcome.REVERT_OUTCOME_REFUSED:
      return "REVERT_OUTCOME_REFUSED";
    case RevertOutcome.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** ImportStatus is what became of one memory of a batch. */
export enum ImportStatus {
  IMPORT_STATUS_UNSPECIFIED = 0,
  /** IMPORT_STATUS_QUEUED - Accepted and queued for writing. */
  IMPORT_STATUS_QUEUED = 1,
  /** IMPORT_STATUS_REJECTED - The entry itself was not usable; `errors` says what about it. */
  IMPORT_STATUS_REJECTED = 2,
  /**
   * IMPORT_STATUS_ERROR - The entry was usable but could not be queued. Resubmitting it is the remedy;
   * an import is written under an identity derived from its own content, so a
   * memory that did land is not duplicated by sending it again.
   */
  IMPORT_STATUS_ERROR = 3,
  /**
   * IMPORT_STATUS_DUPLICATE - The content is already in memory, so nothing was written and nothing was
   * charged. Sending the same batch again is safe and free.
   */
  IMPORT_STATUS_DUPLICATE = 4,
  UNRECOGNIZED = -1,
}

export function importStatusFromJSON(object: any): ImportStatus {
  switch (object) {
    case 0:
    case "IMPORT_STATUS_UNSPECIFIED":
      return ImportStatus.IMPORT_STATUS_UNSPECIFIED;
    case 1:
    case "IMPORT_STATUS_QUEUED":
      return ImportStatus.IMPORT_STATUS_QUEUED;
    case 2:
    case "IMPORT_STATUS_REJECTED":
      return ImportStatus.IMPORT_STATUS_REJECTED;
    case 3:
    case "IMPORT_STATUS_ERROR":
      return ImportStatus.IMPORT_STATUS_ERROR;
    case 4:
    case "IMPORT_STATUS_DUPLICATE":
      return ImportStatus.IMPORT_STATUS_DUPLICATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ImportStatus.UNRECOGNIZED;
  }
}

export function importStatusToJSON(object: ImportStatus): string {
  switch (object) {
    case ImportStatus.IMPORT_STATUS_UNSPECIFIED:
      return "IMPORT_STATUS_UNSPECIFIED";
    case ImportStatus.IMPORT_STATUS_QUEUED:
      return "IMPORT_STATUS_QUEUED";
    case ImportStatus.IMPORT_STATUS_REJECTED:
      return "IMPORT_STATUS_REJECTED";
    case ImportStatus.IMPORT_STATUS_ERROR:
      return "IMPORT_STATUS_ERROR";
    case ImportStatus.IMPORT_STATUS_DUPLICATE:
      return "IMPORT_STATUS_DUPLICATE";
    case ImportStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Instructions is the model-facing guidance accompanying a result, held apart
 * from the data so a caller can render its own document or use this one. Each
 * part arrives already rendered for its domain; a part with nothing to say is
 * empty.
 */
export interface Instructions {
  /** content states what the result is, or what happened. */
  content: string;
  /**
   * policy explains how a policy result outranks the rest. Present only when
   * one was returned.
   */
  policy: string;
  /** adding says how to contribute back to what came back. */
  adding: string;
  /** rating says how to rate what came back. */
  rating: string;
  /**
   * next names the follow-up call this result enables, such as the handle that
   * undoes a write.
   */
  next: string;
}

/**
 * Tag is one selector on a search or a write. The server lowercases each field
 * and folds hyphens to underscores, so "Go" and "go" name the same tag.
 *
 * Which types exist, which of them narrow results rather than boost them, and
 * which carry a version are per-domain: ListDomains describes them.
 */
export interface Tag {
  /** type is the tag's category, such as "language" or "framework". Required. */
  type: string;
  /** value is the value within that category, such as "go". Required. */
  value: string;
  /**
   * version is the version of the thing named, where its type carries one — a
   * version on a type that does not is dropped. Optional, and absent is the
   * common case: most tag types carry no version at all.
   */
  version?: string | undefined;
}

/**
 * DomainEntry describes one memory domain: what it holds, when to draw on it,
 * what belongs in it, and the tag vocabulary it uses.
 */
export interface DomainEntry {
  /** slug is the value a caller passes as the domain field. */
  slug: string;
  title: string;
  summary: string;
  whenToSearch: string;
  whenToSave: string;
  whatNotToSave: string;
  tagsDescription: string;
  /**
   * filter_tag_types name the tag types that narrow results rather than boost
   * them — the distinction deciding whether a wrong tag returns nothing.
   */
  filterTagTypes: string[];
  /**
   * version_tag_types name the tag types that carry a version. A version on any
   * other type is dropped.
   */
  versionTagTypes: string[];
  /**
   * max_tags_per_query bounds `tags` on a call naming this domain. Like
   * max_sources it TRIMS rather than refuses, and it is per-domain rather than
   * global, which is why it sits here rather than on Limits. Zero means the
   * domain sets no cap.
   */
  maxTagsPerQuery: number;
}

export interface ListDomainsRequest {
}

export interface ListDomainsResponse {
  domains: DomainEntry[];
  instructions?: Instructions | undefined;
  limits?:
    | Limits
    | undefined;
  /**
   * deprecated marks what the caller is using as superseded — either this API
   * version, or the SDK build they are running. A client should surface it once
   * per process rather than per call.
   *
   * It does NOT say which of the two it is: that distinction lives in
   * deprecation_message, because only the server knows the remedy and only the
   * server can change the wording without an SDK release. Clients relay the
   * message; they should not try to infer a cause from the flag.
   *
   * This field ships before it is needed on purpose. A client can only be told
   * it is out of date if the version it was built against already carried the
   * field, so adding it when v2 arrives would reach only the callers who least
   * need telling.
   */
  deprecated: boolean;
  /**
   * deprecation_message is the server-authored remedy, empty when not
   * deprecated. It is the whole of what a client should show.
   */
  deprecationMessage: string;
  /**
   * sunset_date is when what the caller uses stops working, as YYYY-MM-DD.
   * Empty when no date is set, which is not a promise that none will be.
   */
  sunsetDate: string;
  /**
   * server_commit names the build that answered, so a caller can identify it in
   * a bug report.
   */
  serverCommit: string;
}

/**
 * Limits are the caps the server enforces, published so a client can check its
 * input before spending a round trip on a call that would be refused.
 *
 * A server that predates this field sends no Limits at all. Treat that as
 * "validate nothing" rather than as zeros, or every call is rejected locally.
 *
 * Note which caps REFUSE and which TRIM. A validator that raises on all of them
 * rejects calls the server would have accepted.
 */
export interface Limits {
  /**
   * max_query_characters bounds `query` on both Search and CreateMemory.
   * Exceeding it is refused with INVALID_ARGUMENT.
   */
  maxQueryCharacters: number;
  /**
   * max_text_characters bounds `title` and `content` TOGETHER, not each.
   * Exceeding it is refused with INVALID_ARGUMENT.
   */
  maxTextCharacters: number;
  /**
   * max_idx_characters bounds any idx-shaped handle: `idx`, `memory_idx`, and
   * each entry of `sources`. Exceeding it is refused with INVALID_ARGUMENT.
   */
  maxIdxCharacters: number;
  /**
   * max_sources bounds `sources` on EnrichMemory. It TRIMS rather than refuses:
   * the server keeps the first max_sources and drops the rest, so a client
   * should trim too rather than raise.
   */
  maxSources: number;
  /**
   * max_feedback_entries bounds `feedback` entries per ShareFeedback call.
   * Exceeding it is refused with INVALID_ARGUMENT.
   */
  maxFeedbackEntries: number;
  /**
   * The four import caps. Each REFUSES rather than trims, so a client should
   * split a batch that exceeds one rather than send it and have it rejected.
   * max_import_memories bounds `memories` per ImportMemories call; the other
   * three bound one entry of it.
   */
  maxImportMemories: number;
  maxImportQueriesPerMemory: number;
  maxImportInsightsPerMemory: number;
  maxImportTagsPerMemory: number;
}

export interface StartSessionRequest {
  /** domain is required: a session belongs to one memory domain. */
  domain: string;
}

export interface StartSessionResponse {
  sessionId: string;
  instructions?: Instructions | undefined;
}

export interface SearchRequest {
  /**
   * tags narrow or boost the results. Call ListDomains for the tag types a
   * domain uses and which of them filter rather than boost.
   */
  tags: Tag[];
  /**
   * domain is required unless session_id is set, which supplies the domain of
   * the session it names.
   */
  domain: string;
  /** query is a task-based question, statement or task description. */
  query: string;
  /**
   * session_id records this search under an open session. Omitting it opens a
   * new one, returned as session_id on the response.
   */
  sessionId: string;
}

export interface SearchResponse {
  sessionId: string;
  /**
   * memories holds what was selected, in rendered order. A search that selected
   * nothing leaves it empty.
   */
  memories: MemoryResult[];
  /** notice is a remark about the query itself rather than about its results. */
  notice: string;
  instructions?: Instructions | undefined;
}

/**
 * MemoryResult is one memory a search returned, with the insights sampled from
 * it.
 */
export interface MemoryResult {
  idx: string;
  kind: string;
  /**
   * times_served counts this delivery along with the previous ones. It records
   * deliveries and nothing else — a frequently served result is not thereby a
   * correct one, which is what endorsed and disputed answer.
   */
  timesServed: number;
  /**
   * intents are the questions this memory has been retrieved by, oldest first.
   * They describe what it answers; none of them is "the one that matched",
   * since a memory is retrieved whole.
   */
  intents: string[];
  insights: InsightResult[];
  /**
   * reference names the idx an earlier search in this session returned this
   * memory under. An entry carrying one carries no insights: the memory is
   * already in the caller's context under that idx, and GetMemory returns it
   * again for a caller that never saw it.
   */
  reference: string;
}

/** InsightResult is one insight held under its memory. */
export interface InsightResult {
  idx: string;
  title: string;
  content: string;
  /**
   * updated is the day the insight was last written, as YYYY-MM-DD. Enrichment
   * edits an insight in place, so it is the age of the knowledge rather than of
   * the record.
   */
  updated: string;
  timesServed: number;
  /**
   * endorsed and disputed are what ShareFeedback has recorded about this
   * insight — the reliability signal times_served is not.
   */
  endorsed: number;
  disputed: number;
}

export interface GetMemoryRequest {
  /**
   * idx is copied exactly as it appeared in a search response. An insight's idx
   * returns the memory holding it.
   */
  idx: string;
}

export interface GetMemoryResponse {
  memory?: MemoryResult | undefined;
}

export interface CreateMemoryRequest {
  tags: Tag[];
  source: DataSource;
  /** domain is required unless session_id is set. */
  domain: string;
  /** session_id names the session this memory was learned during. */
  sessionId: string;
  /** query describes what someone would search to find this memory. */
  query: string;
  title: string;
  content: string;
}

export interface CreateMemoryResponse {
  /**
   * operation_id addresses the write, for RevertMemory. It is empty when none
   * could be minted: the content is worth more than the ability to undo it, so
   * numbering failure degrades to an un-revertible write rather than a
   * rejected one.
   */
  operationId: string;
  instructions?: Instructions | undefined;
}

export interface EnrichMemoryRequest {
  tags: Tag[];
  source: DataSource;
  title: string;
  content: string;
  /** sources name the memories this addition draws on. */
  sources: string[];
  /**
   * session_id is required: the memory being enriched was returned by a search
   * under it, and the session supplies the domain.
   */
  sessionId: string;
  /** memory_idx is the memory to enrich, or "new" to open one. */
  memoryIdx: string;
}

export interface EnrichMemoryResponse {
  operationId: string;
  instructions?: Instructions | undefined;
}

export interface ShareFeedbackRequest {
  /** feedback holds one rating per result being rated. */
  feedback: FeedbackRating[];
  /** session_id is the search being rated. It supplies the domain. */
  sessionId: string;
}

/** FeedbackRating is one rating a caller gives a result. */
export interface FeedbackRating {
  /**
   * idx is copied exactly as it appeared in a search response, and cannot be
   * constructed by hand. An insight's idx rates that insight; a memory's own
   * idx rates every insight under it. Required.
   */
  idx: string;
  /** relevant reports whether the result was a good match for the query. */
  relevant: boolean;
  /** correct reports whether its content was accurate. */
  correct: boolean;
  /** comment is an optional note about this result. */
  comment?: string | undefined;
}

export interface ShareFeedbackResponse {
  sessionId: string;
  entries: FeedbackEntry[];
  instructions?: Instructions | undefined;
}

/** FeedbackEntry is one rating that was recorded. */
export interface FeedbackEntry {
  idx: string;
  relevant: boolean;
  correct: boolean;
  /**
   * advice is the suggestion this particular verdict earned, empty when the
   * verdict suggests nothing. It addresses one result, so it belongs to the
   * entry rather than to the batch.
   */
  advice: string;
}

export interface RevertMemoryRequest {
  /** op_id is the operation id CreateMemory or EnrichMemory returned. */
  opId: string;
}

export interface RevertMemoryResponse {
  operationId: string;
  outcome: RevertOutcome;
  instructions?: Instructions | undefined;
}

export interface ImportMemoriesRequest {
  /**
   * domain is required unless session_id is set, which supplies the domain of
   * the session it names. Every memory of the batch is imported into that one
   * domain; a batch cannot span domains.
   */
  domain: string;
  memories: ImportedMemory[];
  /**
   * session_id names the session this knowledge was contributed during, so a
   * batch gathered in the course of a task belongs to that series of work
   * rather than standing on its own. Omitting it files the memories under no
   * session, which is what a standalone upload wants.
   */
  sessionId: string;
}

/**
 * ImportedMemory is one memory to contribute: what it should be found by, and
 * what it holds.
 */
export interface ImportedMemory {
  /**
   * queries are what someone would search to find this memory. At least one is
   * required.
   */
  queries: string[];
  /** insights are what the memory holds. At least one is required. */
  insights: ImportedInsight[];
  tags: Tag[];
}

export interface ImportedInsight {
  title: string;
  content: string;
}

export interface ImportMemoriesResponse {
  /** results holds one entry per memory submitted, in the order they were sent. */
  results: ImportOutcome[];
  instructions?: Instructions | undefined;
}

/**
 * ImportOutcome is what happened to one memory of the batch, addressed by its
 * position in the request: an import mints no handle a caller could name it by.
 */
export interface ImportOutcome {
  index: number;
  status: ImportStatus;
  /**
   * errors says what was wrong with an entry that was not queued, and is empty
   * for one that was.
   */
  errors: string[];
}

function createBaseInstructions(): Instructions {
  return { content: "", policy: "", adding: "", rating: "", next: "" };
}

export const Instructions: MessageFns<Instructions> = {
  encode(message: Instructions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.content !== "") {
      writer.uint32(10).string(message.content);
    }
    if (message.policy !== "") {
      writer.uint32(18).string(message.policy);
    }
    if (message.adding !== "") {
      writer.uint32(26).string(message.adding);
    }
    if (message.rating !== "") {
      writer.uint32(34).string(message.rating);
    }
    if (message.next !== "") {
      writer.uint32(42).string(message.next);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Instructions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInstructions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policy = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.adding = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.rating = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.next = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Instructions {
    return {
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      policy: isSet(object.policy) ? globalThis.String(object.policy) : "",
      adding: isSet(object.adding) ? globalThis.String(object.adding) : "",
      rating: isSet(object.rating) ? globalThis.String(object.rating) : "",
      next: isSet(object.next) ? globalThis.String(object.next) : "",
    };
  },

  toJSON(message: Instructions): unknown {
    const obj: any = {};
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.policy !== "") {
      obj.policy = message.policy;
    }
    if (message.adding !== "") {
      obj.adding = message.adding;
    }
    if (message.rating !== "") {
      obj.rating = message.rating;
    }
    if (message.next !== "") {
      obj.next = message.next;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Instructions>, I>>(base?: I): Instructions {
    return Instructions.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Instructions>, I>>(object: I): Instructions {
    const message = createBaseInstructions();
    message.content = object.content ?? "";
    message.policy = object.policy ?? "";
    message.adding = object.adding ?? "";
    message.rating = object.rating ?? "";
    message.next = object.next ?? "";
    return message;
  },
};

function createBaseTag(): Tag {
  return { type: "", value: "", version: undefined };
}

export const Tag: MessageFns<Tag> = {
  encode(message: Tag, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== "") {
      writer.uint32(10).string(message.type);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    if (message.version !== undefined) {
      writer.uint32(26).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Tag {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTag();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.type = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Tag {
    return {
      type: isSet(object.type) ? globalThis.String(object.type) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      version: isSet(object.version) ? globalThis.String(object.version) : undefined,
    };
  },

  toJSON(message: Tag): unknown {
    const obj: any = {};
    if (message.type !== "") {
      obj.type = message.type;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.version !== undefined) {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Tag>, I>>(base?: I): Tag {
    return Tag.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Tag>, I>>(object: I): Tag {
    const message = createBaseTag();
    message.type = object.type ?? "";
    message.value = object.value ?? "";
    message.version = object.version ?? undefined;
    return message;
  },
};

function createBaseDomainEntry(): DomainEntry {
  return {
    slug: "",
    title: "",
    summary: "",
    whenToSearch: "",
    whenToSave: "",
    whatNotToSave: "",
    tagsDescription: "",
    filterTagTypes: [],
    versionTagTypes: [],
    maxTagsPerQuery: 0,
  };
}

export const DomainEntry: MessageFns<DomainEntry> = {
  encode(message: DomainEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.slug !== "") {
      writer.uint32(10).string(message.slug);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.summary !== "") {
      writer.uint32(26).string(message.summary);
    }
    if (message.whenToSearch !== "") {
      writer.uint32(34).string(message.whenToSearch);
    }
    if (message.whenToSave !== "") {
      writer.uint32(42).string(message.whenToSave);
    }
    if (message.whatNotToSave !== "") {
      writer.uint32(50).string(message.whatNotToSave);
    }
    if (message.tagsDescription !== "") {
      writer.uint32(58).string(message.tagsDescription);
    }
    for (const v of message.filterTagTypes) {
      writer.uint32(66).string(v!);
    }
    for (const v of message.versionTagTypes) {
      writer.uint32(74).string(v!);
    }
    if (message.maxTagsPerQuery !== 0) {
      writer.uint32(80).int32(message.maxTagsPerQuery);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DomainEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDomainEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.slug = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.summary = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.whenToSearch = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.whenToSave = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.whatNotToSave = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.tagsDescription = reader.string();
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.filterTagTypes.push(reader.string());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.versionTagTypes.push(reader.string());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.maxTagsPerQuery = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DomainEntry {
    return {
      slug: isSet(object.slug) ? globalThis.String(object.slug) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      summary: isSet(object.summary) ? globalThis.String(object.summary) : "",
      whenToSearch: isSet(object.whenToSearch) ? globalThis.String(object.whenToSearch) : "",
      whenToSave: isSet(object.whenToSave) ? globalThis.String(object.whenToSave) : "",
      whatNotToSave: isSet(object.whatNotToSave) ? globalThis.String(object.whatNotToSave) : "",
      tagsDescription: isSet(object.tagsDescription) ? globalThis.String(object.tagsDescription) : "",
      filterTagTypes: globalThis.Array.isArray(object?.filterTagTypes)
        ? object.filterTagTypes.map((e: any) => globalThis.String(e))
        : [],
      versionTagTypes: globalThis.Array.isArray(object?.versionTagTypes)
        ? object.versionTagTypes.map((e: any) => globalThis.String(e))
        : [],
      maxTagsPerQuery: isSet(object.maxTagsPerQuery) ? globalThis.Number(object.maxTagsPerQuery) : 0,
    };
  },

  toJSON(message: DomainEntry): unknown {
    const obj: any = {};
    if (message.slug !== "") {
      obj.slug = message.slug;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.summary !== "") {
      obj.summary = message.summary;
    }
    if (message.whenToSearch !== "") {
      obj.whenToSearch = message.whenToSearch;
    }
    if (message.whenToSave !== "") {
      obj.whenToSave = message.whenToSave;
    }
    if (message.whatNotToSave !== "") {
      obj.whatNotToSave = message.whatNotToSave;
    }
    if (message.tagsDescription !== "") {
      obj.tagsDescription = message.tagsDescription;
    }
    if (message.filterTagTypes?.length) {
      obj.filterTagTypes = message.filterTagTypes;
    }
    if (message.versionTagTypes?.length) {
      obj.versionTagTypes = message.versionTagTypes;
    }
    if (message.maxTagsPerQuery !== 0) {
      obj.maxTagsPerQuery = Math.round(message.maxTagsPerQuery);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DomainEntry>, I>>(base?: I): DomainEntry {
    return DomainEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DomainEntry>, I>>(object: I): DomainEntry {
    const message = createBaseDomainEntry();
    message.slug = object.slug ?? "";
    message.title = object.title ?? "";
    message.summary = object.summary ?? "";
    message.whenToSearch = object.whenToSearch ?? "";
    message.whenToSave = object.whenToSave ?? "";
    message.whatNotToSave = object.whatNotToSave ?? "";
    message.tagsDescription = object.tagsDescription ?? "";
    message.filterTagTypes = object.filterTagTypes?.map((e) => e) || [];
    message.versionTagTypes = object.versionTagTypes?.map((e) => e) || [];
    message.maxTagsPerQuery = object.maxTagsPerQuery ?? 0;
    return message;
  },
};

function createBaseListDomainsRequest(): ListDomainsRequest {
  return {};
}

export const ListDomainsRequest: MessageFns<ListDomainsRequest> = {
  encode(_: ListDomainsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDomainsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDomainsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ListDomainsRequest {
    return {};
  },

  toJSON(_: ListDomainsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDomainsRequest>, I>>(base?: I): ListDomainsRequest {
    return ListDomainsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDomainsRequest>, I>>(_: I): ListDomainsRequest {
    const message = createBaseListDomainsRequest();
    return message;
  },
};

function createBaseListDomainsResponse(): ListDomainsResponse {
  return {
    domains: [],
    instructions: undefined,
    limits: undefined,
    deprecated: false,
    deprecationMessage: "",
    sunsetDate: "",
    serverCommit: "",
  };
}

export const ListDomainsResponse: MessageFns<ListDomainsResponse> = {
  encode(message: ListDomainsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.domains) {
      DomainEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    if (message.limits !== undefined) {
      Limits.encode(message.limits, writer.uint32(26).fork()).join();
    }
    if (message.deprecated !== false) {
      writer.uint32(32).bool(message.deprecated);
    }
    if (message.deprecationMessage !== "") {
      writer.uint32(42).string(message.deprecationMessage);
    }
    if (message.sunsetDate !== "") {
      writer.uint32(50).string(message.sunsetDate);
    }
    if (message.serverCommit !== "") {
      writer.uint32(58).string(message.serverCommit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListDomainsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListDomainsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domains.push(DomainEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.limits = Limits.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deprecated = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.deprecationMessage = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sunsetDate = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.serverCommit = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListDomainsResponse {
    return {
      domains: globalThis.Array.isArray(object?.domains) ? object.domains.map((e: any) => DomainEntry.fromJSON(e)) : [],
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
      limits: isSet(object.limits) ? Limits.fromJSON(object.limits) : undefined,
      deprecated: isSet(object.deprecated) ? globalThis.Boolean(object.deprecated) : false,
      deprecationMessage: isSet(object.deprecationMessage) ? globalThis.String(object.deprecationMessage) : "",
      sunsetDate: isSet(object.sunsetDate) ? globalThis.String(object.sunsetDate) : "",
      serverCommit: isSet(object.serverCommit) ? globalThis.String(object.serverCommit) : "",
    };
  },

  toJSON(message: ListDomainsResponse): unknown {
    const obj: any = {};
    if (message.domains?.length) {
      obj.domains = message.domains.map((e) => DomainEntry.toJSON(e));
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    if (message.limits !== undefined) {
      obj.limits = Limits.toJSON(message.limits);
    }
    if (message.deprecated !== false) {
      obj.deprecated = message.deprecated;
    }
    if (message.deprecationMessage !== "") {
      obj.deprecationMessage = message.deprecationMessage;
    }
    if (message.sunsetDate !== "") {
      obj.sunsetDate = message.sunsetDate;
    }
    if (message.serverCommit !== "") {
      obj.serverCommit = message.serverCommit;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListDomainsResponse>, I>>(base?: I): ListDomainsResponse {
    return ListDomainsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListDomainsResponse>, I>>(object: I): ListDomainsResponse {
    const message = createBaseListDomainsResponse();
    message.domains = object.domains?.map((e) => DomainEntry.fromPartial(e)) || [];
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    message.limits = (object.limits !== undefined && object.limits !== null)
      ? Limits.fromPartial(object.limits)
      : undefined;
    message.deprecated = object.deprecated ?? false;
    message.deprecationMessage = object.deprecationMessage ?? "";
    message.sunsetDate = object.sunsetDate ?? "";
    message.serverCommit = object.serverCommit ?? "";
    return message;
  },
};

function createBaseLimits(): Limits {
  return {
    maxQueryCharacters: 0,
    maxTextCharacters: 0,
    maxIdxCharacters: 0,
    maxSources: 0,
    maxFeedbackEntries: 0,
    maxImportMemories: 0,
    maxImportQueriesPerMemory: 0,
    maxImportInsightsPerMemory: 0,
    maxImportTagsPerMemory: 0,
  };
}

export const Limits: MessageFns<Limits> = {
  encode(message: Limits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxQueryCharacters !== 0) {
      writer.uint32(8).int32(message.maxQueryCharacters);
    }
    if (message.maxTextCharacters !== 0) {
      writer.uint32(16).int32(message.maxTextCharacters);
    }
    if (message.maxIdxCharacters !== 0) {
      writer.uint32(24).int32(message.maxIdxCharacters);
    }
    if (message.maxSources !== 0) {
      writer.uint32(32).int32(message.maxSources);
    }
    if (message.maxFeedbackEntries !== 0) {
      writer.uint32(40).int32(message.maxFeedbackEntries);
    }
    if (message.maxImportMemories !== 0) {
      writer.uint32(48).int32(message.maxImportMemories);
    }
    if (message.maxImportQueriesPerMemory !== 0) {
      writer.uint32(56).int32(message.maxImportQueriesPerMemory);
    }
    if (message.maxImportInsightsPerMemory !== 0) {
      writer.uint32(64).int32(message.maxImportInsightsPerMemory);
    }
    if (message.maxImportTagsPerMemory !== 0) {
      writer.uint32(72).int32(message.maxImportTagsPerMemory);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Limits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLimits();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxQueryCharacters = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.maxTextCharacters = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.maxIdxCharacters = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxSources = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxFeedbackEntries = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.maxImportMemories = reader.int32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxImportQueriesPerMemory = reader.int32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxImportInsightsPerMemory = reader.int32();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.maxImportTagsPerMemory = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Limits {
    return {
      maxQueryCharacters: isSet(object.maxQueryCharacters) ? globalThis.Number(object.maxQueryCharacters) : 0,
      maxTextCharacters: isSet(object.maxTextCharacters) ? globalThis.Number(object.maxTextCharacters) : 0,
      maxIdxCharacters: isSet(object.maxIdxCharacters) ? globalThis.Number(object.maxIdxCharacters) : 0,
      maxSources: isSet(object.maxSources) ? globalThis.Number(object.maxSources) : 0,
      maxFeedbackEntries: isSet(object.maxFeedbackEntries) ? globalThis.Number(object.maxFeedbackEntries) : 0,
      maxImportMemories: isSet(object.maxImportMemories) ? globalThis.Number(object.maxImportMemories) : 0,
      maxImportQueriesPerMemory: isSet(object.maxImportQueriesPerMemory)
        ? globalThis.Number(object.maxImportQueriesPerMemory)
        : 0,
      maxImportInsightsPerMemory: isSet(object.maxImportInsightsPerMemory)
        ? globalThis.Number(object.maxImportInsightsPerMemory)
        : 0,
      maxImportTagsPerMemory: isSet(object.maxImportTagsPerMemory)
        ? globalThis.Number(object.maxImportTagsPerMemory)
        : 0,
    };
  },

  toJSON(message: Limits): unknown {
    const obj: any = {};
    if (message.maxQueryCharacters !== 0) {
      obj.maxQueryCharacters = Math.round(message.maxQueryCharacters);
    }
    if (message.maxTextCharacters !== 0) {
      obj.maxTextCharacters = Math.round(message.maxTextCharacters);
    }
    if (message.maxIdxCharacters !== 0) {
      obj.maxIdxCharacters = Math.round(message.maxIdxCharacters);
    }
    if (message.maxSources !== 0) {
      obj.maxSources = Math.round(message.maxSources);
    }
    if (message.maxFeedbackEntries !== 0) {
      obj.maxFeedbackEntries = Math.round(message.maxFeedbackEntries);
    }
    if (message.maxImportMemories !== 0) {
      obj.maxImportMemories = Math.round(message.maxImportMemories);
    }
    if (message.maxImportQueriesPerMemory !== 0) {
      obj.maxImportQueriesPerMemory = Math.round(message.maxImportQueriesPerMemory);
    }
    if (message.maxImportInsightsPerMemory !== 0) {
      obj.maxImportInsightsPerMemory = Math.round(message.maxImportInsightsPerMemory);
    }
    if (message.maxImportTagsPerMemory !== 0) {
      obj.maxImportTagsPerMemory = Math.round(message.maxImportTagsPerMemory);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Limits>, I>>(base?: I): Limits {
    return Limits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Limits>, I>>(object: I): Limits {
    const message = createBaseLimits();
    message.maxQueryCharacters = object.maxQueryCharacters ?? 0;
    message.maxTextCharacters = object.maxTextCharacters ?? 0;
    message.maxIdxCharacters = object.maxIdxCharacters ?? 0;
    message.maxSources = object.maxSources ?? 0;
    message.maxFeedbackEntries = object.maxFeedbackEntries ?? 0;
    message.maxImportMemories = object.maxImportMemories ?? 0;
    message.maxImportQueriesPerMemory = object.maxImportQueriesPerMemory ?? 0;
    message.maxImportInsightsPerMemory = object.maxImportInsightsPerMemory ?? 0;
    message.maxImportTagsPerMemory = object.maxImportTagsPerMemory ?? 0;
    return message;
  },
};

function createBaseStartSessionRequest(): StartSessionRequest {
  return { domain: "" };
}

export const StartSessionRequest: MessageFns<StartSessionRequest> = {
  encode(message: StartSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartSessionRequest {
    return { domain: isSet(object.domain) ? globalThis.String(object.domain) : "" };
  },

  toJSON(message: StartSessionRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSessionRequest>, I>>(base?: I): StartSessionRequest {
    return StartSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSessionRequest>, I>>(object: I): StartSessionRequest {
    const message = createBaseStartSessionRequest();
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseStartSessionResponse(): StartSessionResponse {
  return { sessionId: "", instructions: undefined };
}

export const StartSessionResponse: MessageFns<StartSessionResponse> = {
  encode(message: StartSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartSessionResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartSessionResponse {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: StartSessionResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StartSessionResponse>, I>>(base?: I): StartSessionResponse {
    return StartSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StartSessionResponse>, I>>(object: I): StartSessionResponse {
    const message = createBaseStartSessionResponse();
    message.sessionId = object.sessionId ?? "";
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseSearchRequest(): SearchRequest {
  return { tags: [], domain: "", query: "", sessionId: "" };
}

export const SearchRequest: MessageFns<SearchRequest> = {
  encode(message: SearchRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.query !== "") {
      writer.uint32(26).string(message.query);
    }
    if (message.sessionId !== "") {
      writer.uint32(34).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchRequest {
    return {
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
    };
  },

  toJSON(message: SearchRequest): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchRequest>, I>>(base?: I): SearchRequest {
    return SearchRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchRequest>, I>>(object: I): SearchRequest {
    const message = createBaseSearchRequest();
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    message.domain = object.domain ?? "";
    message.query = object.query ?? "";
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseSearchResponse(): SearchResponse {
  return { sessionId: "", memories: [], notice: "", instructions: undefined };
}

export const SearchResponse: MessageFns<SearchResponse> = {
  encode(message: SearchResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    for (const v of message.memories) {
      MemoryResult.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.notice !== "") {
      writer.uint32(26).string(message.notice);
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SearchResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSearchResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.memories.push(MemoryResult.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.notice = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SearchResponse {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      memories: globalThis.Array.isArray(object?.memories)
        ? object.memories.map((e: any) => MemoryResult.fromJSON(e))
        : [],
      notice: isSet(object.notice) ? globalThis.String(object.notice) : "",
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: SearchResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.memories?.length) {
      obj.memories = message.memories.map((e) => MemoryResult.toJSON(e));
    }
    if (message.notice !== "") {
      obj.notice = message.notice;
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SearchResponse>, I>>(base?: I): SearchResponse {
    return SearchResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SearchResponse>, I>>(object: I): SearchResponse {
    const message = createBaseSearchResponse();
    message.sessionId = object.sessionId ?? "";
    message.memories = object.memories?.map((e) => MemoryResult.fromPartial(e)) || [];
    message.notice = object.notice ?? "";
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseMemoryResult(): MemoryResult {
  return { idx: "", kind: "", timesServed: 0, intents: [], insights: [], reference: "" };
}

export const MemoryResult: MessageFns<MemoryResult> = {
  encode(message: MemoryResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idx !== "") {
      writer.uint32(10).string(message.idx);
    }
    if (message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.timesServed !== 0) {
      writer.uint32(24).int64(message.timesServed);
    }
    for (const v of message.intents) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.insights) {
      InsightResult.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.reference !== "") {
      writer.uint32(50).string(message.reference);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MemoryResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMemoryResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idx = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.timesServed = longToNumber(reader.int64());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.intents.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.insights.push(InsightResult.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.reference = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MemoryResult {
    return {
      idx: isSet(object.idx) ? globalThis.String(object.idx) : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      timesServed: isSet(object.timesServed) ? globalThis.Number(object.timesServed) : 0,
      intents: globalThis.Array.isArray(object?.intents) ? object.intents.map((e: any) => globalThis.String(e)) : [],
      insights: globalThis.Array.isArray(object?.insights)
        ? object.insights.map((e: any) => InsightResult.fromJSON(e))
        : [],
      reference: isSet(object.reference) ? globalThis.String(object.reference) : "",
    };
  },

  toJSON(message: MemoryResult): unknown {
    const obj: any = {};
    if (message.idx !== "") {
      obj.idx = message.idx;
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.timesServed !== 0) {
      obj.timesServed = Math.round(message.timesServed);
    }
    if (message.intents?.length) {
      obj.intents = message.intents;
    }
    if (message.insights?.length) {
      obj.insights = message.insights.map((e) => InsightResult.toJSON(e));
    }
    if (message.reference !== "") {
      obj.reference = message.reference;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MemoryResult>, I>>(base?: I): MemoryResult {
    return MemoryResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MemoryResult>, I>>(object: I): MemoryResult {
    const message = createBaseMemoryResult();
    message.idx = object.idx ?? "";
    message.kind = object.kind ?? "";
    message.timesServed = object.timesServed ?? 0;
    message.intents = object.intents?.map((e) => e) || [];
    message.insights = object.insights?.map((e) => InsightResult.fromPartial(e)) || [];
    message.reference = object.reference ?? "";
    return message;
  },
};

function createBaseInsightResult(): InsightResult {
  return { idx: "", title: "", content: "", updated: "", timesServed: 0, endorsed: 0, disputed: 0 };
}

export const InsightResult: MessageFns<InsightResult> = {
  encode(message: InsightResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idx !== "") {
      writer.uint32(10).string(message.idx);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(26).string(message.content);
    }
    if (message.updated !== "") {
      writer.uint32(34).string(message.updated);
    }
    if (message.timesServed !== 0) {
      writer.uint32(40).int64(message.timesServed);
    }
    if (message.endorsed !== 0) {
      writer.uint32(48).int64(message.endorsed);
    }
    if (message.disputed !== 0) {
      writer.uint32(56).int64(message.disputed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): InsightResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseInsightResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idx = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.updated = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.timesServed = longToNumber(reader.int64());
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.endorsed = longToNumber(reader.int64());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.disputed = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): InsightResult {
    return {
      idx: isSet(object.idx) ? globalThis.String(object.idx) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      updated: isSet(object.updated) ? globalThis.String(object.updated) : "",
      timesServed: isSet(object.timesServed) ? globalThis.Number(object.timesServed) : 0,
      endorsed: isSet(object.endorsed) ? globalThis.Number(object.endorsed) : 0,
      disputed: isSet(object.disputed) ? globalThis.Number(object.disputed) : 0,
    };
  },

  toJSON(message: InsightResult): unknown {
    const obj: any = {};
    if (message.idx !== "") {
      obj.idx = message.idx;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.updated !== "") {
      obj.updated = message.updated;
    }
    if (message.timesServed !== 0) {
      obj.timesServed = Math.round(message.timesServed);
    }
    if (message.endorsed !== 0) {
      obj.endorsed = Math.round(message.endorsed);
    }
    if (message.disputed !== 0) {
      obj.disputed = Math.round(message.disputed);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<InsightResult>, I>>(base?: I): InsightResult {
    return InsightResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<InsightResult>, I>>(object: I): InsightResult {
    const message = createBaseInsightResult();
    message.idx = object.idx ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.updated = object.updated ?? "";
    message.timesServed = object.timesServed ?? 0;
    message.endorsed = object.endorsed ?? 0;
    message.disputed = object.disputed ?? 0;
    return message;
  },
};

function createBaseGetMemoryRequest(): GetMemoryRequest {
  return { idx: "" };
}

export const GetMemoryRequest: MessageFns<GetMemoryRequest> = {
  encode(message: GetMemoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idx !== "") {
      writer.uint32(10).string(message.idx);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMemoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMemoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idx = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMemoryRequest {
    return { idx: isSet(object.idx) ? globalThis.String(object.idx) : "" };
  },

  toJSON(message: GetMemoryRequest): unknown {
    const obj: any = {};
    if (message.idx !== "") {
      obj.idx = message.idx;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMemoryRequest>, I>>(base?: I): GetMemoryRequest {
    return GetMemoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMemoryRequest>, I>>(object: I): GetMemoryRequest {
    const message = createBaseGetMemoryRequest();
    message.idx = object.idx ?? "";
    return message;
  },
};

function createBaseGetMemoryResponse(): GetMemoryResponse {
  return { memory: undefined };
}

export const GetMemoryResponse: MessageFns<GetMemoryResponse> = {
  encode(message: GetMemoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.memory !== undefined) {
      MemoryResult.encode(message.memory, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetMemoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetMemoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.memory = MemoryResult.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetMemoryResponse {
    return { memory: isSet(object.memory) ? MemoryResult.fromJSON(object.memory) : undefined };
  },

  toJSON(message: GetMemoryResponse): unknown {
    const obj: any = {};
    if (message.memory !== undefined) {
      obj.memory = MemoryResult.toJSON(message.memory);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetMemoryResponse>, I>>(base?: I): GetMemoryResponse {
    return GetMemoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetMemoryResponse>, I>>(object: I): GetMemoryResponse {
    const message = createBaseGetMemoryResponse();
    message.memory = (object.memory !== undefined && object.memory !== null)
      ? MemoryResult.fromPartial(object.memory)
      : undefined;
    return message;
  },
};

function createBaseCreateMemoryRequest(): CreateMemoryRequest {
  return { tags: [], source: 0, domain: "", sessionId: "", query: "", title: "", content: "" };
}

export const CreateMemoryRequest: MessageFns<CreateMemoryRequest> = {
  encode(message: CreateMemoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.source !== 0) {
      writer.uint32(16).int32(message.source);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    if (message.sessionId !== "") {
      writer.uint32(34).string(message.sessionId);
    }
    if (message.query !== "") {
      writer.uint32(42).string(message.query);
    }
    if (message.title !== "") {
      writer.uint32(50).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(58).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateMemoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateMemoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.query = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateMemoryRequest {
    return {
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
      source: isSet(object.source) ? dataSourceFromJSON(object.source) : 0,
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      query: isSet(object.query) ? globalThis.String(object.query) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: CreateMemoryRequest): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    if (message.source !== 0) {
      obj.source = dataSourceToJSON(message.source);
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.query !== "") {
      obj.query = message.query;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateMemoryRequest>, I>>(base?: I): CreateMemoryRequest {
    return CreateMemoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateMemoryRequest>, I>>(object: I): CreateMemoryRequest {
    const message = createBaseCreateMemoryRequest();
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    message.source = object.source ?? 0;
    message.domain = object.domain ?? "";
    message.sessionId = object.sessionId ?? "";
    message.query = object.query ?? "";
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseCreateMemoryResponse(): CreateMemoryResponse {
  return { operationId: "", instructions: undefined };
}

export const CreateMemoryResponse: MessageFns<CreateMemoryResponse> = {
  encode(message: CreateMemoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operationId !== "") {
      writer.uint32(10).string(message.operationId);
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateMemoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateMemoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateMemoryResponse {
    return {
      operationId: isSet(object.operationId) ? globalThis.String(object.operationId) : "",
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: CreateMemoryResponse): unknown {
    const obj: any = {};
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateMemoryResponse>, I>>(base?: I): CreateMemoryResponse {
    return CreateMemoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateMemoryResponse>, I>>(object: I): CreateMemoryResponse {
    const message = createBaseCreateMemoryResponse();
    message.operationId = object.operationId ?? "";
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseEnrichMemoryRequest(): EnrichMemoryRequest {
  return { tags: [], source: 0, title: "", content: "", sources: [], sessionId: "", memoryIdx: "" };
}

export const EnrichMemoryRequest: MessageFns<EnrichMemoryRequest> = {
  encode(message: EnrichMemoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.source !== 0) {
      writer.uint32(16).int32(message.source);
    }
    if (message.title !== "") {
      writer.uint32(26).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(34).string(message.content);
    }
    for (const v of message.sources) {
      writer.uint32(42).string(v!);
    }
    if (message.sessionId !== "") {
      writer.uint32(50).string(message.sessionId);
    }
    if (message.memoryIdx !== "") {
      writer.uint32(58).string(message.memoryIdx);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnrichMemoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnrichMemoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.content = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sources.push(reader.string());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.memoryIdx = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnrichMemoryRequest {
    return {
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
      source: isSet(object.source) ? dataSourceFromJSON(object.source) : 0,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
      sources: globalThis.Array.isArray(object?.sources) ? object.sources.map((e: any) => globalThis.String(e)) : [],
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      memoryIdx: isSet(object.memoryIdx) ? globalThis.String(object.memoryIdx) : "",
    };
  },

  toJSON(message: EnrichMemoryRequest): unknown {
    const obj: any = {};
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    if (message.source !== 0) {
      obj.source = dataSourceToJSON(message.source);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    if (message.sources?.length) {
      obj.sources = message.sources;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.memoryIdx !== "") {
      obj.memoryIdx = message.memoryIdx;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EnrichMemoryRequest>, I>>(base?: I): EnrichMemoryRequest {
    return EnrichMemoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EnrichMemoryRequest>, I>>(object: I): EnrichMemoryRequest {
    const message = createBaseEnrichMemoryRequest();
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    message.source = object.source ?? 0;
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    message.sources = object.sources?.map((e) => e) || [];
    message.sessionId = object.sessionId ?? "";
    message.memoryIdx = object.memoryIdx ?? "";
    return message;
  },
};

function createBaseEnrichMemoryResponse(): EnrichMemoryResponse {
  return { operationId: "", instructions: undefined };
}

export const EnrichMemoryResponse: MessageFns<EnrichMemoryResponse> = {
  encode(message: EnrichMemoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operationId !== "") {
      writer.uint32(10).string(message.operationId);
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EnrichMemoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEnrichMemoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EnrichMemoryResponse {
    return {
      operationId: isSet(object.operationId) ? globalThis.String(object.operationId) : "",
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: EnrichMemoryResponse): unknown {
    const obj: any = {};
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EnrichMemoryResponse>, I>>(base?: I): EnrichMemoryResponse {
    return EnrichMemoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EnrichMemoryResponse>, I>>(object: I): EnrichMemoryResponse {
    const message = createBaseEnrichMemoryResponse();
    message.operationId = object.operationId ?? "";
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseShareFeedbackRequest(): ShareFeedbackRequest {
  return { feedback: [], sessionId: "" };
}

export const ShareFeedbackRequest: MessageFns<ShareFeedbackRequest> = {
  encode(message: ShareFeedbackRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.feedback) {
      FeedbackRating.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShareFeedbackRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShareFeedbackRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.feedback.push(FeedbackRating.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShareFeedbackRequest {
    return {
      feedback: globalThis.Array.isArray(object?.feedback)
        ? object.feedback.map((e: any) => FeedbackRating.fromJSON(e))
        : [],
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
    };
  },

  toJSON(message: ShareFeedbackRequest): unknown {
    const obj: any = {};
    if (message.feedback?.length) {
      obj.feedback = message.feedback.map((e) => FeedbackRating.toJSON(e));
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShareFeedbackRequest>, I>>(base?: I): ShareFeedbackRequest {
    return ShareFeedbackRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShareFeedbackRequest>, I>>(object: I): ShareFeedbackRequest {
    const message = createBaseShareFeedbackRequest();
    message.feedback = object.feedback?.map((e) => FeedbackRating.fromPartial(e)) || [];
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseFeedbackRating(): FeedbackRating {
  return { idx: "", relevant: false, correct: false, comment: undefined };
}

export const FeedbackRating: MessageFns<FeedbackRating> = {
  encode(message: FeedbackRating, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idx !== "") {
      writer.uint32(10).string(message.idx);
    }
    if (message.relevant !== false) {
      writer.uint32(16).bool(message.relevant);
    }
    if (message.correct !== false) {
      writer.uint32(24).bool(message.correct);
    }
    if (message.comment !== undefined) {
      writer.uint32(34).string(message.comment);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FeedbackRating {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeedbackRating();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idx = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.relevant = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.correct = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.comment = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeedbackRating {
    return {
      idx: isSet(object.idx) ? globalThis.String(object.idx) : "",
      relevant: isSet(object.relevant) ? globalThis.Boolean(object.relevant) : false,
      correct: isSet(object.correct) ? globalThis.Boolean(object.correct) : false,
      comment: isSet(object.comment) ? globalThis.String(object.comment) : undefined,
    };
  },

  toJSON(message: FeedbackRating): unknown {
    const obj: any = {};
    if (message.idx !== "") {
      obj.idx = message.idx;
    }
    if (message.relevant !== false) {
      obj.relevant = message.relevant;
    }
    if (message.correct !== false) {
      obj.correct = message.correct;
    }
    if (message.comment !== undefined) {
      obj.comment = message.comment;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FeedbackRating>, I>>(base?: I): FeedbackRating {
    return FeedbackRating.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FeedbackRating>, I>>(object: I): FeedbackRating {
    const message = createBaseFeedbackRating();
    message.idx = object.idx ?? "";
    message.relevant = object.relevant ?? false;
    message.correct = object.correct ?? false;
    message.comment = object.comment ?? undefined;
    return message;
  },
};

function createBaseShareFeedbackResponse(): ShareFeedbackResponse {
  return { sessionId: "", entries: [], instructions: undefined };
}

export const ShareFeedbackResponse: MessageFns<ShareFeedbackResponse> = {
  encode(message: ShareFeedbackResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== "") {
      writer.uint32(10).string(message.sessionId);
    }
    for (const v of message.entries) {
      FeedbackEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShareFeedbackResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShareFeedbackResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.entries.push(FeedbackEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShareFeedbackResponse {
    return {
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => FeedbackEntry.fromJSON(e))
        : [],
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: ShareFeedbackResponse): unknown {
    const obj: any = {};
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => FeedbackEntry.toJSON(e));
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShareFeedbackResponse>, I>>(base?: I): ShareFeedbackResponse {
    return ShareFeedbackResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShareFeedbackResponse>, I>>(object: I): ShareFeedbackResponse {
    const message = createBaseShareFeedbackResponse();
    message.sessionId = object.sessionId ?? "";
    message.entries = object.entries?.map((e) => FeedbackEntry.fromPartial(e)) || [];
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseFeedbackEntry(): FeedbackEntry {
  return { idx: "", relevant: false, correct: false, advice: "" };
}

export const FeedbackEntry: MessageFns<FeedbackEntry> = {
  encode(message: FeedbackEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idx !== "") {
      writer.uint32(10).string(message.idx);
    }
    if (message.relevant !== false) {
      writer.uint32(16).bool(message.relevant);
    }
    if (message.correct !== false) {
      writer.uint32(24).bool(message.correct);
    }
    if (message.advice !== "") {
      writer.uint32(34).string(message.advice);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FeedbackEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFeedbackEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.idx = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.relevant = reader.bool();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.correct = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.advice = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): FeedbackEntry {
    return {
      idx: isSet(object.idx) ? globalThis.String(object.idx) : "",
      relevant: isSet(object.relevant) ? globalThis.Boolean(object.relevant) : false,
      correct: isSet(object.correct) ? globalThis.Boolean(object.correct) : false,
      advice: isSet(object.advice) ? globalThis.String(object.advice) : "",
    };
  },

  toJSON(message: FeedbackEntry): unknown {
    const obj: any = {};
    if (message.idx !== "") {
      obj.idx = message.idx;
    }
    if (message.relevant !== false) {
      obj.relevant = message.relevant;
    }
    if (message.correct !== false) {
      obj.correct = message.correct;
    }
    if (message.advice !== "") {
      obj.advice = message.advice;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FeedbackEntry>, I>>(base?: I): FeedbackEntry {
    return FeedbackEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FeedbackEntry>, I>>(object: I): FeedbackEntry {
    const message = createBaseFeedbackEntry();
    message.idx = object.idx ?? "";
    message.relevant = object.relevant ?? false;
    message.correct = object.correct ?? false;
    message.advice = object.advice ?? "";
    return message;
  },
};

function createBaseRevertMemoryRequest(): RevertMemoryRequest {
  return { opId: "" };
}

export const RevertMemoryRequest: MessageFns<RevertMemoryRequest> = {
  encode(message: RevertMemoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.opId !== "") {
      writer.uint32(10).string(message.opId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevertMemoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevertMemoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.opId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevertMemoryRequest {
    return { opId: isSet(object.opId) ? globalThis.String(object.opId) : "" };
  },

  toJSON(message: RevertMemoryRequest): unknown {
    const obj: any = {};
    if (message.opId !== "") {
      obj.opId = message.opId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevertMemoryRequest>, I>>(base?: I): RevertMemoryRequest {
    return RevertMemoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevertMemoryRequest>, I>>(object: I): RevertMemoryRequest {
    const message = createBaseRevertMemoryRequest();
    message.opId = object.opId ?? "";
    return message;
  },
};

function createBaseRevertMemoryResponse(): RevertMemoryResponse {
  return { operationId: "", outcome: 0, instructions: undefined };
}

export const RevertMemoryResponse: MessageFns<RevertMemoryResponse> = {
  encode(message: RevertMemoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.operationId !== "") {
      writer.uint32(10).string(message.operationId);
    }
    if (message.outcome !== 0) {
      writer.uint32(16).int32(message.outcome);
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RevertMemoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRevertMemoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.operationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.outcome = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RevertMemoryResponse {
    return {
      operationId: isSet(object.operationId) ? globalThis.String(object.operationId) : "",
      outcome: isSet(object.outcome) ? revertOutcomeFromJSON(object.outcome) : 0,
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: RevertMemoryResponse): unknown {
    const obj: any = {};
    if (message.operationId !== "") {
      obj.operationId = message.operationId;
    }
    if (message.outcome !== 0) {
      obj.outcome = revertOutcomeToJSON(message.outcome);
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RevertMemoryResponse>, I>>(base?: I): RevertMemoryResponse {
    return RevertMemoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RevertMemoryResponse>, I>>(object: I): RevertMemoryResponse {
    const message = createBaseRevertMemoryResponse();
    message.operationId = object.operationId ?? "";
    message.outcome = object.outcome ?? 0;
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseImportMemoriesRequest(): ImportMemoriesRequest {
  return { domain: "", memories: [], sessionId: "" };
}

export const ImportMemoriesRequest: MessageFns<ImportMemoriesRequest> = {
  encode(message: ImportMemoriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    for (const v of message.memories) {
      ImportedMemory.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.sessionId !== "") {
      writer.uint32(26).string(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportMemoriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportMemoriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.memories.push(ImportedMemory.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportMemoriesRequest {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      memories: globalThis.Array.isArray(object?.memories)
        ? object.memories.map((e: any) => ImportedMemory.fromJSON(e))
        : [],
      sessionId: isSet(object.sessionId) ? globalThis.String(object.sessionId) : "",
    };
  },

  toJSON(message: ImportMemoriesRequest): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.memories?.length) {
      obj.memories = message.memories.map((e) => ImportedMemory.toJSON(e));
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportMemoriesRequest>, I>>(base?: I): ImportMemoriesRequest {
    return ImportMemoriesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportMemoriesRequest>, I>>(object: I): ImportMemoriesRequest {
    const message = createBaseImportMemoriesRequest();
    message.domain = object.domain ?? "";
    message.memories = object.memories?.map((e) => ImportedMemory.fromPartial(e)) || [];
    message.sessionId = object.sessionId ?? "";
    return message;
  },
};

function createBaseImportedMemory(): ImportedMemory {
  return { queries: [], insights: [], tags: [] };
}

export const ImportedMemory: MessageFns<ImportedMemory> = {
  encode(message: ImportedMemory, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.queries) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.insights) {
      ImportedInsight.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.tags) {
      Tag.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportedMemory {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportedMemory();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.queries.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.insights.push(ImportedInsight.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tags.push(Tag.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportedMemory {
    return {
      queries: globalThis.Array.isArray(object?.queries) ? object.queries.map((e: any) => globalThis.String(e)) : [],
      insights: globalThis.Array.isArray(object?.insights)
        ? object.insights.map((e: any) => ImportedInsight.fromJSON(e))
        : [],
      tags: globalThis.Array.isArray(object?.tags) ? object.tags.map((e: any) => Tag.fromJSON(e)) : [],
    };
  },

  toJSON(message: ImportedMemory): unknown {
    const obj: any = {};
    if (message.queries?.length) {
      obj.queries = message.queries;
    }
    if (message.insights?.length) {
      obj.insights = message.insights.map((e) => ImportedInsight.toJSON(e));
    }
    if (message.tags?.length) {
      obj.tags = message.tags.map((e) => Tag.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportedMemory>, I>>(base?: I): ImportedMemory {
    return ImportedMemory.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportedMemory>, I>>(object: I): ImportedMemory {
    const message = createBaseImportedMemory();
    message.queries = object.queries?.map((e) => e) || [];
    message.insights = object.insights?.map((e) => ImportedInsight.fromPartial(e)) || [];
    message.tags = object.tags?.map((e) => Tag.fromPartial(e)) || [];
    return message;
  },
};

function createBaseImportedInsight(): ImportedInsight {
  return { title: "", content: "" };
}

export const ImportedInsight: MessageFns<ImportedInsight> = {
  encode(message: ImportedInsight, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.content !== "") {
      writer.uint32(18).string(message.content);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportedInsight {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportedInsight();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.content = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportedInsight {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      content: isSet(object.content) ? globalThis.String(object.content) : "",
    };
  },

  toJSON(message: ImportedInsight): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.content !== "") {
      obj.content = message.content;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportedInsight>, I>>(base?: I): ImportedInsight {
    return ImportedInsight.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportedInsight>, I>>(object: I): ImportedInsight {
    const message = createBaseImportedInsight();
    message.title = object.title ?? "";
    message.content = object.content ?? "";
    return message;
  },
};

function createBaseImportMemoriesResponse(): ImportMemoriesResponse {
  return { results: [], instructions: undefined };
}

export const ImportMemoriesResponse: MessageFns<ImportMemoriesResponse> = {
  encode(message: ImportMemoriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      ImportOutcome.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.instructions !== undefined) {
      Instructions.encode(message.instructions, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportMemoriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportMemoriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(ImportOutcome.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.instructions = Instructions.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportMemoriesResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => ImportOutcome.fromJSON(e))
        : [],
      instructions: isSet(object.instructions) ? Instructions.fromJSON(object.instructions) : undefined,
    };
  },

  toJSON(message: ImportMemoriesResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => ImportOutcome.toJSON(e));
    }
    if (message.instructions !== undefined) {
      obj.instructions = Instructions.toJSON(message.instructions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportMemoriesResponse>, I>>(base?: I): ImportMemoriesResponse {
    return ImportMemoriesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportMemoriesResponse>, I>>(object: I): ImportMemoriesResponse {
    const message = createBaseImportMemoriesResponse();
    message.results = object.results?.map((e) => ImportOutcome.fromPartial(e)) || [];
    message.instructions = (object.instructions !== undefined && object.instructions !== null)
      ? Instructions.fromPartial(object.instructions)
      : undefined;
    return message;
  },
};

function createBaseImportOutcome(): ImportOutcome {
  return { index: 0, status: 0, errors: [] };
}

export const ImportOutcome: MessageFns<ImportOutcome> = {
  encode(message: ImportOutcome, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).int32(message.index);
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    for (const v of message.errors) {
      writer.uint32(26).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportOutcome {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportOutcome();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.index = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.errors.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportOutcome {
    return {
      index: isSet(object.index) ? globalThis.Number(object.index) : 0,
      status: isSet(object.status) ? importStatusFromJSON(object.status) : 0,
      errors: globalThis.Array.isArray(object?.errors) ? object.errors.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: ImportOutcome): unknown {
    const obj: any = {};
    if (message.index !== 0) {
      obj.index = Math.round(message.index);
    }
    if (message.status !== 0) {
      obj.status = importStatusToJSON(message.status);
    }
    if (message.errors?.length) {
      obj.errors = message.errors;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ImportOutcome>, I>>(base?: I): ImportOutcome {
    return ImportOutcome.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ImportOutcome>, I>>(object: I): ImportOutcome {
    const message = createBaseImportOutcome();
    message.index = object.index ?? 0;
    message.status = object.status ?? 0;
    message.errors = object.errors?.map((e) => e) || [];
    return message;
  },
};

/**
 * MemoryService is the shared memory surface: the operations a client uses to
 * find knowledge, contribute to it, and rate what it was given.
 *
 * Every identifier crossing this boundary is an external handle a previous
 * response issued — "session-<h>", "create-<h>", "memory-<h>-N" — never a row
 * id. A handle addresses the memory domain it belongs to, which is why most
 * operations take no domain of their own.
 */
export type MemoryServiceService = typeof MemoryServiceService;
export const MemoryServiceService = {
  /**
   * ListDomains returns the memory domains the caller may name, together
   * with the limits the server enforces. It takes no domain itself: it is the
   * answer to "which domain?", and the one call a client makes before any other.
   */
  listDomains: {
    path: "/memcoai.memory.v1.MemoryService/ListDomains",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ListDomainsRequest) => Buffer.from(ListDomainsRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => ListDomainsRequest.decode(value),
    responseSerialize: (value: ListDomainsResponse) => Buffer.from(ListDomainsResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => ListDomainsResponse.decode(value),
  },
  /**
   * StartSession opens a session. Every search made under it is recorded as
   * one series, which is what relates the searches an agent runs for a task.
   */
  startSession: {
    path: "/memcoai.memory.v1.MemoryService/StartSession",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: StartSessionRequest) => Buffer.from(StartSessionRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => StartSessionRequest.decode(value),
    responseSerialize: (value: StartSessionResponse) => Buffer.from(StartSessionResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => StartSessionResponse.decode(value),
  },
  /**
   * Search retrieves memories for a task-based query, recording the search
   * against a session so it can be rated afterwards.
   */
  search: {
    path: "/memcoai.memory.v1.MemoryService/Search",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: SearchRequest) => Buffer.from(SearchRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => SearchRequest.decode(value),
    responseSerialize: (value: SearchResponse) => Buffer.from(SearchResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => SearchResponse.decode(value),
  },
  /**
   * GetMemory returns the memory behind an idx a search delivered, including
   * one returned as a reference to an earlier search in the same session.
   */
  getMemory: {
    path: "/memcoai.memory.v1.MemoryService/GetMemory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetMemoryRequest) => Buffer.from(GetMemoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => GetMemoryRequest.decode(value),
    responseSerialize: (value: GetMemoryResponse) => Buffer.from(GetMemoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => GetMemoryResponse.decode(value),
  },
  /**
   * CreateMemory saves new knowledge. It is accepted asynchronously, so the
   * response carries the operation id addressing the write rather than the
   * memory it will become.
   */
  createMemory: {
    path: "/memcoai.memory.v1.MemoryService/CreateMemory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: CreateMemoryRequest) => Buffer.from(CreateMemoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => CreateMemoryRequest.decode(value),
    responseSerialize: (value: CreateMemoryResponse) => Buffer.from(CreateMemoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => CreateMemoryResponse.decode(value),
  },
  /** EnrichMemory adds to a memory a search returned, or opens a new one. */
  enrichMemory: {
    path: "/memcoai.memory.v1.MemoryService/EnrichMemory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: EnrichMemoryRequest) => Buffer.from(EnrichMemoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => EnrichMemoryRequest.decode(value),
    responseSerialize: (value: EnrichMemoryResponse) => Buffer.from(EnrichMemoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => EnrichMemoryResponse.decode(value),
  },
  /**
   * ShareFeedback rates the results of one search. Ratings are what move the
   * reliability signal on an insight.
   */
  shareFeedback: {
    path: "/memcoai.memory.v1.MemoryService/ShareFeedback",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ShareFeedbackRequest) => Buffer.from(ShareFeedbackRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => ShareFeedbackRequest.decode(value),
    responseSerialize: (value: ShareFeedbackResponse) => Buffer.from(ShareFeedbackResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => ShareFeedbackResponse.decode(value),
  },
  /**
   * RevertMemory undoes the caller's own write, addressed by the operation id
   * that write returned. Every outcome is a successful call: expired, refused
   * and not_found report a caller-visible state, not a service failure.
   */
  revertMemory: {
    path: "/memcoai.memory.v1.MemoryService/RevertMemory",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: RevertMemoryRequest) => Buffer.from(RevertMemoryRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => RevertMemoryRequest.decode(value),
    responseSerialize: (value: RevertMemoryResponse) => Buffer.from(RevertMemoryResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => RevertMemoryResponse.decode(value),
  },
  /**
   * ImportMemories contributes many memories in one call. Each becomes an
   * ordinary memory — evaluated on the way in and carrying the contributor's own
   * reliability, exactly as CreateMemory does — so this is a way to write a lot
   * at once, not a way to write differently.
   *
   * Every memory is judged on its own, so a refused entry does not stop the
   * others, and each is written asynchronously: the response reports what was
   * accepted rather than what now exists.
   *
   * A batch carries no operation id. RevertMemory addresses a single write, and
   * there is no handle that undoes an import.
   *
   * Naming a session records the batch against it, as CreateMemory and
   * EnrichMemory do, and supplies the domain the memories are imported into.
   */
  importMemories: {
    path: "/memcoai.memory.v1.MemoryService/ImportMemories",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ImportMemoriesRequest) => Buffer.from(ImportMemoriesRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer) => ImportMemoriesRequest.decode(value),
    responseSerialize: (value: ImportMemoriesResponse) => Buffer.from(ImportMemoriesResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer) => ImportMemoriesResponse.decode(value),
  },
} as const;

export interface MemoryServiceServer extends UntypedServiceImplementation {
  /**
   * ListDomains returns the memory domains the caller may name, together
   * with the limits the server enforces. It takes no domain itself: it is the
   * answer to "which domain?", and the one call a client makes before any other.
   */
  listDomains: handleUnaryCall<ListDomainsRequest, ListDomainsResponse>;
  /**
   * StartSession opens a session. Every search made under it is recorded as
   * one series, which is what relates the searches an agent runs for a task.
   */
  startSession: handleUnaryCall<StartSessionRequest, StartSessionResponse>;
  /**
   * Search retrieves memories for a task-based query, recording the search
   * against a session so it can be rated afterwards.
   */
  search: handleUnaryCall<SearchRequest, SearchResponse>;
  /**
   * GetMemory returns the memory behind an idx a search delivered, including
   * one returned as a reference to an earlier search in the same session.
   */
  getMemory: handleUnaryCall<GetMemoryRequest, GetMemoryResponse>;
  /**
   * CreateMemory saves new knowledge. It is accepted asynchronously, so the
   * response carries the operation id addressing the write rather than the
   * memory it will become.
   */
  createMemory: handleUnaryCall<CreateMemoryRequest, CreateMemoryResponse>;
  /** EnrichMemory adds to a memory a search returned, or opens a new one. */
  enrichMemory: handleUnaryCall<EnrichMemoryRequest, EnrichMemoryResponse>;
  /**
   * ShareFeedback rates the results of one search. Ratings are what move the
   * reliability signal on an insight.
   */
  shareFeedback: handleUnaryCall<ShareFeedbackRequest, ShareFeedbackResponse>;
  /**
   * RevertMemory undoes the caller's own write, addressed by the operation id
   * that write returned. Every outcome is a successful call: expired, refused
   * and not_found report a caller-visible state, not a service failure.
   */
  revertMemory: handleUnaryCall<RevertMemoryRequest, RevertMemoryResponse>;
  /**
   * ImportMemories contributes many memories in one call. Each becomes an
   * ordinary memory — evaluated on the way in and carrying the contributor's own
   * reliability, exactly as CreateMemory does — so this is a way to write a lot
   * at once, not a way to write differently.
   *
   * Every memory is judged on its own, so a refused entry does not stop the
   * others, and each is written asynchronously: the response reports what was
   * accepted rather than what now exists.
   *
   * A batch carries no operation id. RevertMemory addresses a single write, and
   * there is no handle that undoes an import.
   *
   * Naming a session records the batch against it, as CreateMemory and
   * EnrichMemory do, and supplies the domain the memories are imported into.
   */
  importMemories: handleUnaryCall<ImportMemoriesRequest, ImportMemoriesResponse>;
}

export interface MemoryServiceClient extends Client {
  /**
   * ListDomains returns the memory domains the caller may name, together
   * with the limits the server enforces. It takes no domain itself: it is the
   * answer to "which domain?", and the one call a client makes before any other.
   */
  listDomains(
    request: ListDomainsRequest,
    callback: (error: ServiceError | null, response: ListDomainsResponse) => void,
  ): ClientUnaryCall;
  listDomains(
    request: ListDomainsRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListDomainsResponse) => void,
  ): ClientUnaryCall;
  listDomains(
    request: ListDomainsRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListDomainsResponse) => void,
  ): ClientUnaryCall;
  /**
   * StartSession opens a session. Every search made under it is recorded as
   * one series, which is what relates the searches an agent runs for a task.
   */
  startSession(
    request: StartSessionRequest,
    callback: (error: ServiceError | null, response: StartSessionResponse) => void,
  ): ClientUnaryCall;
  startSession(
    request: StartSessionRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StartSessionResponse) => void,
  ): ClientUnaryCall;
  startSession(
    request: StartSessionRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StartSessionResponse) => void,
  ): ClientUnaryCall;
  /**
   * Search retrieves memories for a task-based query, recording the search
   * against a session so it can be rated afterwards.
   */
  search(
    request: SearchRequest,
    callback: (error: ServiceError | null, response: SearchResponse) => void,
  ): ClientUnaryCall;
  search(
    request: SearchRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: SearchResponse) => void,
  ): ClientUnaryCall;
  search(
    request: SearchRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: SearchResponse) => void,
  ): ClientUnaryCall;
  /**
   * GetMemory returns the memory behind an idx a search delivered, including
   * one returned as a reference to an earlier search in the same session.
   */
  getMemory(
    request: GetMemoryRequest,
    callback: (error: ServiceError | null, response: GetMemoryResponse) => void,
  ): ClientUnaryCall;
  getMemory(
    request: GetMemoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetMemoryResponse) => void,
  ): ClientUnaryCall;
  getMemory(
    request: GetMemoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetMemoryResponse) => void,
  ): ClientUnaryCall;
  /**
   * CreateMemory saves new knowledge. It is accepted asynchronously, so the
   * response carries the operation id addressing the write rather than the
   * memory it will become.
   */
  createMemory(
    request: CreateMemoryRequest,
    callback: (error: ServiceError | null, response: CreateMemoryResponse) => void,
  ): ClientUnaryCall;
  createMemory(
    request: CreateMemoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateMemoryResponse) => void,
  ): ClientUnaryCall;
  createMemory(
    request: CreateMemoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateMemoryResponse) => void,
  ): ClientUnaryCall;
  /** EnrichMemory adds to a memory a search returned, or opens a new one. */
  enrichMemory(
    request: EnrichMemoryRequest,
    callback: (error: ServiceError | null, response: EnrichMemoryResponse) => void,
  ): ClientUnaryCall;
  enrichMemory(
    request: EnrichMemoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: EnrichMemoryResponse) => void,
  ): ClientUnaryCall;
  enrichMemory(
    request: EnrichMemoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: EnrichMemoryResponse) => void,
  ): ClientUnaryCall;
  /**
   * ShareFeedback rates the results of one search. Ratings are what move the
   * reliability signal on an insight.
   */
  shareFeedback(
    request: ShareFeedbackRequest,
    callback: (error: ServiceError | null, response: ShareFeedbackResponse) => void,
  ): ClientUnaryCall;
  shareFeedback(
    request: ShareFeedbackRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ShareFeedbackResponse) => void,
  ): ClientUnaryCall;
  shareFeedback(
    request: ShareFeedbackRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ShareFeedbackResponse) => void,
  ): ClientUnaryCall;
  /**
   * RevertMemory undoes the caller's own write, addressed by the operation id
   * that write returned. Every outcome is a successful call: expired, refused
   * and not_found report a caller-visible state, not a service failure.
   */
  revertMemory(
    request: RevertMemoryRequest,
    callback: (error: ServiceError | null, response: RevertMemoryResponse) => void,
  ): ClientUnaryCall;
  revertMemory(
    request: RevertMemoryRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: RevertMemoryResponse) => void,
  ): ClientUnaryCall;
  revertMemory(
    request: RevertMemoryRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: RevertMemoryResponse) => void,
  ): ClientUnaryCall;
  /**
   * ImportMemories contributes many memories in one call. Each becomes an
   * ordinary memory — evaluated on the way in and carrying the contributor's own
   * reliability, exactly as CreateMemory does — so this is a way to write a lot
   * at once, not a way to write differently.
   *
   * Every memory is judged on its own, so a refused entry does not stop the
   * others, and each is written asynchronously: the response reports what was
   * accepted rather than what now exists.
   *
   * A batch carries no operation id. RevertMemory addresses a single write, and
   * there is no handle that undoes an import.
   *
   * Naming a session records the batch against it, as CreateMemory and
   * EnrichMemory do, and supplies the domain the memories are imported into.
   */
  importMemories(
    request: ImportMemoriesRequest,
    callback: (error: ServiceError | null, response: ImportMemoriesResponse) => void,
  ): ClientUnaryCall;
  importMemories(
    request: ImportMemoriesRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ImportMemoriesResponse) => void,
  ): ClientUnaryCall;
  importMemories(
    request: ImportMemoriesRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ImportMemoriesResponse) => void,
  ): ClientUnaryCall;
}

export const MemoryServiceClient = makeGenericClientConstructor(
  MemoryServiceService,
  "memcoai.memory.v1.MemoryService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): MemoryServiceClient;
  service: typeof MemoryServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
