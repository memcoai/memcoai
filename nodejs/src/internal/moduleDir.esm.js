// Copied to <out>/src/internal/moduleDir.js by the ESM and test builds.
// See moduleDir.d.ts for why this is not TypeScript.
// `import.meta.dirname` needs Node >= 20.11; `engines.node` is >= 22.
export const moduleDir = import.meta.dirname
