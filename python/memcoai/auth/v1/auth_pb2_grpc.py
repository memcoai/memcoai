# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from memcoai.auth.v1 import auth_pb2 as memcoai_dot_auth_dot_v1_dot_auth__pb2

GRPC_GENERATED_VERSION = '1.76.0'
GRPC_VERSION = grpc.__version__
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    raise RuntimeError(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + ' but the generated code in memcoai/auth/v1/auth_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
    )


class TokenServiceStub(object):
    """TokenService turns an API client's credentials into a bearer token.

    It is the one service called without a token of its own — the credentials in
    the request are what authenticate it — so it lives apart from the services that
    require one, and a client can bootstrap over gRPC with no HTTP client at all.
    The same exchange is served over REST at /api/external/v1/oauth/token.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.IssueToken = channel.unary_unary(
                '/memcoai.auth.v1.TokenService/IssueToken',
                request_serializer=memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenRequest.SerializeToString,
                response_deserializer=memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenResponse.FromString,
                _registered_method=True)


class TokenServiceServicer(object):
    """TokenService turns an API client's credentials into a bearer token.

    It is the one service called without a token of its own — the credentials in
    the request are what authenticate it — so it lives apart from the services that
    require one, and a client can bootstrap over gRPC with no HTTP client at all.
    The same exchange is served over REST at /api/external/v1/oauth/token.
    """

    def IssueToken(self, request, context):
        """IssueToken performs the OAuth 2.0 client-credentials grant (RFC 6749 §4.4).

        The token is a bearer credential for the operations the client was created
        for, and expires on its own. Every credential failure — an unknown client, a
        wrong secret, a retired one — is the same UNAUTHENTICATED answer, so nothing
        about which part was wrong is disclosed.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_TokenServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'IssueToken': grpc.unary_unary_rpc_method_handler(
                    servicer.IssueToken,
                    request_deserializer=memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenRequest.FromString,
                    response_serializer=memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'memcoai.auth.v1.TokenService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('memcoai.auth.v1.TokenService', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class TokenService(object):
    """TokenService turns an API client's credentials into a bearer token.

    It is the one service called without a token of its own — the credentials in
    the request are what authenticate it — so it lives apart from the services that
    require one, and a client can bootstrap over gRPC with no HTTP client at all.
    The same exchange is served over REST at /api/external/v1/oauth/token.
    """

    @staticmethod
    def IssueToken(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/memcoai.auth.v1.TokenService/IssueToken',
            memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenRequest.SerializeToString,
            memcoai_dot_auth_dot_v1_dot_auth__pb2.IssueTokenResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
