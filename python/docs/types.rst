Types
=====

Every method returns a frozen dataclass rather than a protobuf message, so the
generated code stays an implementation detail and values arrive in their
natural Python form.

.. automodule:: memcoai.types
   :members:
   :member-order: bysource
